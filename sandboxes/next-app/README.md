# Next.js — App Router

Provider in a client component, anchors in server components, route-aware steps.

> **Generated — do not edit.** This is a standalone copy of
> [`examples/next-app`](../../examples/next-app), rewritten so it installs from npm
> instead of the workspace. Change the example, then run `pnpm sandboxes`.

```bash
npm install
npm run dev
```

Open [`walkthrough/flows.ts`](walkthrough/flows.ts) to see the tour itself — it is
plain data, and every anchor it points at is a typed identifier rather than a
CSS selector.

Then try breaking it: rename an anchor in the registry and run `npx cairn check`.
